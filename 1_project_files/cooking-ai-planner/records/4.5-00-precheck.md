# 小饭桌 4.5 Step 00 执行记录

日期：2026-05-24

## 执行目标

在开始 4.5 UI 与功能优化前，检查当前 4.0 项目是否可构建、AI 接入文件是否存在、菜谱详情页路由是否可复用、核心 localStorage 数据是否已在项目中使用，并标记 4.5 后续需要谨慎修改的模块。

## 修改原因

4.5 将连续修改成长任务、省钱计划、AI 结果展示、待购清单、计时器和菜谱详情相关体验。执行前需要确认当前基线可运行，避免在已有未提交改动上盲目继续修改。

## 实际修改

本步骤不修改业务代码，仅新增本次预检查 record。

检查结果：

- 当前项目目录存在：`api/`、`src/`、`public/`、`docs/`、`records/`、`dist/`、`node_modules/`。
- 4.0 AI 接入相关文件存在：
  - `api/ai.js`
  - `src/services/aiService.js`
  - `src/data/mockAIResponse.js`
  - `records/`
- 当前使用 `HashRouter`，已有菜谱详情页路由：`/recipes/:recipeId`。
- 当前已有任务详情页路由：`/tasks/:taskId`。
- 当前已有待购清单路由：`/shopping` 与 `/shopping/:itemId`。

## 涉及文件

- 新增：`1_project_files/cooking-ai-planner/records/4.5-00-precheck.md`
- 检查：`1_project_files/cooking-ai-planner/src/App.jsx`
- 检查：`1_project_files/cooking-ai-planner/src/hooks/useTaskProgress.js`
- 检查：`1_project_files/cooking-ai-planner/src/hooks/useBudgetLedger.js`
- 检查：`1_project_files/cooking-ai-planner/src/store/storageKeys.js`
- 检查：`1_project_files/cooking-ai-planner/src/pages/HomePage.jsx`
- 检查：`1_project_files/cooking-ai-planner/src/pages/RecipeDetailPage.jsx`
- 检查：`1_project_files/cooking-ai-planner/src/pages/ShoppingListPage.jsx`
- 检查：`1_project_files/cooking-ai-planner/src/services/shoppingService.js`
- 检查：`1_project_files/cooking-ai-planner/src/services/aiShoppingListService.js`
- 检查：`1_project_files/cooking-ai-planner/src/services/aiFavoriteRecipeService.js`

## 数据影响

本步骤无数据结构改动。

当前已确认的 localStorage / 本地状态相关数据：

- 待购清单：`cooking_ai_planner.shopping.items.v1`
- 收藏菜谱 ID：`cooking_ai_planner.favorites.ids.v1`
- AI 收藏菜谱：`xiaofanzhuo_favorite_recipes`
- AI 生成待购清单原始快照：`xiaofanzhuo_ai_shopping_list`
- 首页生成器状态：`cooking_ai_planner.recipe_generator.state.v1`
- 最近做过：`cooking_ai_planner.recent.cooked.v1`
- AI 配置：`cooking_ai_planner.ai.config.v1`
- 推荐任务经验：`cooking_ai_planner.v3.task_progress.v1`
- 省钱计划账本：`cooking_ai_planner.v3.budget_ledger.v1`

需要注意：

- 当前随机一道菜只在 `recipeGenUi.randomRecipe` 中保存为内存状态，尚未确认有独立历史记录 localStorage。
- 当前待购清单存在 `AI_SHOPPING_GROUP_ID = 'ai-generated'`，4.5 Step 8 将移除“AI 生成清单”模块，需要迁移或调整 AI 食材归组逻辑。
- 当前任务经验使用 `totalXp` 推导等级，后续 Step 1 需要重点确认完成任务后推荐卡片刷新与升级反馈。

## UI影响

本步骤无 UI 改动。

已识别的 4.5 可能影响 UI 区域：

- 首页推荐任务面板：`RecommendedTasksPanel`
- 任务详情页：`TaskDetailPage`
- 省钱计划弹窗：`BudgetPlanModal`
- 首页 AI 结果卡片：`HomePage`
- 菜谱详情页：`RecipeDetailPage`
- 加入待购清单成功弹窗：`AddToShoppingResultModal`
- 待购清单页与清单详情页：`ShoppingListPage`、`ShoppingDetailPage`
- 随机一道菜弹窗：`RandomRecipeModal`
- 右侧计时器相关首页工具区

## 测试结果

已运行：

```bash
npm.cmd run build
```

结果：

- build：通过
- 输出目录：`dist/`
- 输出文件：
  - `dist/index.html`
  - `dist/assets/index-CB7RBL7r.css`
  - `dist/assets/index-CbaJnyou.js`
- 构建耗时：约 3.72s
- 构建提示：Rolldown 输出 plugin timing warning，属于构建耗时提示，不阻断产物生成。

未运行：

- `npm run preview`：本步骤只要求执行前总检查，未启动本地预览服务。

## 当前状态

4.5 执行前总检查已完成。当前项目可正常构建，4.0 AI 接入文件、详情页路由、records 目录与主要 localStorage 数据路径均已确认。

需要谨慎修改的模块：

- 成长任务：避免只增加 XP 而不刷新推荐任务显示。
- 省钱计划：编辑/删除记录必须重新计算预算统计并同步 localStorage。
- AI 菜谱卡片详情：应复用现有 `/recipes/:recipeId` 详情页和收藏逻辑。
- 待购清单：移除 AI 分组时要避免丢失 AI 生成食材。
- 计时器：新增全局悬浮状态时不要破坏右侧工具区计时入口。
- 步骤辅助图：优先预留服务与缓存，不应阻塞详情页加载。

## 下一步建议

等待用户确认后执行 Step 1：推荐任务刷新与等级经验 bug 修复。下一步应重点检查 `useTaskProgress`、`RecommendedTasksPanel`、`TaskDetailPage` 和任务完成后的刷新逻辑。
