# 小饭桌 4.6 Step 00 执行记录

日期：2026-05-24

## 执行目标

在开始 4.6 文字、布局与动效优化前，检查当前项目是否可构建，并定位后续步骤可能影响的组件、样式和状态文件。

## 修改原因

4.6 属于轻量优化版本，后续步骤会涉及首页文案、推荐任务、主容器边距、侧边导航版本号和微动效。执行前需要先明确现状，避免误改业务逻辑或扩大修改范围。

## 实际修改

- 未修改业务代码。
- 新增本次前置检查记录文件：`records/4.6-00-precheck.md`。
- 执行项目结构、关键文案、关键组件和构建状态检查。

## 涉及文件

- `1_project_files/cooking-ai-planner/src/components/home/HomeHero.jsx`
- `1_project_files/cooking-ai-planner/src/components/home/RecommendedTasksPanel.jsx`
- `1_project_files/cooking-ai-planner/src/components/AppLayout.jsx`
- `1_project_files/cooking-ai-planner/src/components/SideNav.jsx`
- `1_project_files/cooking-ai-planner/src/components/home/FloatingTimer.jsx`
- `1_project_files/cooking-ai-planner/src/components/home/CookingTimerCard.jsx`
- `1_project_files/cooking-ai-planner/src/hooks/useCookingTimer.js`
- `1_project_files/cooking-ai-planner/src/styles/app.css`
- `1_project_files/cooking-ai-planner/src/styles/tokens.css`
- `1_project_files/cooking-ai-planner/records/4.6-00-precheck.md`

## 当前项目结构摘要

```text
1_project_files/cooking-ai-planner
├─ api/                         # Vercel Serverless API
├─ docs/                        # Vercel / AI 测试文档
├─ public/brand/                # logo 与品牌静态资源
├─ records/                     # 4.0 / 4.5 / 4.6 归档记录
├─ src/
│  ├─ components/               # AppLayout、SideNav、HomeHero、任务、计时器等组件
│  ├─ data/                     # 菜谱、任务与 mock 数据
│  ├─ hooks/                    # localStorage、任务进度、预算、计时器等 hooks
│  ├─ pages/                    # 路由页面
│  ├─ services/                 # AI、待购清单、收藏、随机记录、步骤图片等服务
│  ├─ store/                    # 全局本地状态与 storage key
│  └─ styles/                   # app.css 与 tokens.css
├─ package.json
└─ vite.config.js
```

## 文案影响

当前仍存在 4.6 Step 1 需要清理的首页输入区开发文案：

- `小饭桌今日主操作`
- `4.0 AI/demo 流程`
- `无 API Key 时自动使用演示数据`
- `本轮仅占位，不做识别`
- `3.0 后续步骤再接入识别或 RAG`

主要位置：`src/components/home/HomeHero.jsx`。

## UI 影响

后续 4.6 预计影响：

- 首页输入主面板：`HomeHero.jsx` 与 `.homeHero*` 样式。
- 推荐任务模块：`RecommendedTasksPanel.jsx` 与 `.taskPanel*` / `.taskCard*` 样式。
- 主页面右侧内容容器：`AppLayout.jsx`、`.dashboardBody`、`.appShell`、`.appMain`、`.homeLayout` 等样式。
- 左侧导航栏：`SideNav.jsx` 与 `.sideNav*` 样式。
- 全局悬浮计时器：`FloatingTimer.jsx`、`useCookingTimer.js` 与 `.floatingTimer*` 样式。
- 按钮、卡片、弹窗微动效：主要集中在 `app.css`。

## 数据影响

本次前置检查未改动 localStorage 或业务状态。

后续需要谨慎处理的状态：

- 推荐任务进度：`cooking_ai_planner.v3.task_progress.v1`
- 全局计时器：`xiaofanzhuo_global_timer_v1`
- 待购清单：`cooking_ai_planner.shopping.items.v1`
- 随机菜记录：`xiaofanzhuo_random_recipe_history`

## 需要谨慎修改的模块

- `RecommendedTasksPanel.jsx`：Step 2 会调整任务空状态和刷新逻辑，需要保持 4.5 的 XP / 等级修复不回退。
- `SideNav.jsx`：Step 4 要把版本号改为统一配置读取，避免散落硬编码。
- `app.css`：Step 3 和 Step 5 都会改样式，应避免影响移动端布局和已有 3.0/4.5 UI token。
- `useCookingTimer.js` / `FloatingTimer.jsx`：Step 5 只应增强收纳/展开动效，不应改变计时器业务逻辑。

## 测试结果

已执行：

```bash
npm.cmd run build
```

结果：

- build：通过
- 输出目录：`dist/`
- 输出文件：
  - `dist/index.html`
  - `dist/assets/index-Dec0u55c.css`
  - `dist/assets/index-Pv_vaQ5d.js`
- 构建耗时：约 710ms

## 当前状态

4.6 执行前总检查已完成。当前项目可以正常构建，已定位首页输入区、推荐任务、主容器布局、侧边导航、计时器悬浮窗和全局样式文件。

## 下一步建议

等待确认后执行 4.6 Step 1：删除开始做饭输入区中的开发占位文案与内部提示。
