# 小饭桌 4.6 Step 1 执行记录

日期：2026-05-24

## 执行目标

清理“开始做饭”输入区中的开发阶段文案、demo/API Key 提示和占位说明，让首页主操作区更像正式产品界面。

## 修改原因

4.6 要求删除会暴露内部开发阶段的信息，避免用户在首页看到版本号、demo、API Key、识别占位或后续 RAG 计划等非产品化说明。

## 实际修改

- 检查 `HomeHero.jsx` 当前工作区状态。
- 确认以下开发文案已经不再出现在首页输入区：
  - `小饭桌今日主操作`
  - `4.0 AI/demo 流程`
  - `无 API Key 时自动使用演示数据`
  - `本轮仅占位，不做识别`
  - `3.0 后续步骤再接入识别或 RAG`
- 保留输入框、拍照上传按钮、生成菜谱按钮和筛选入口。
- 本步未新增业务逻辑。

## 涉及文件

- `1_project_files/cooking-ai-planner/src/components/home/HomeHero.jsx`
- `1_project_files/cooking-ai-planner/records/4.6-01-remove-dev-placeholder-text.md`

## 文案影响

- 首页输入区不再显示开发阶段说明。
- 首页输入区不再显示 demo、API Key、占位识别、RAG 后续计划等内部提示。
- 用户仍可直接输入做饭需求、上传图片并生成菜谱。

## UI 影响

- 清理文案后，首页输入区视觉更简洁。
- 标题、输入框、按钮和筛选入口保持原有 UI 风格。
- 未发现清理后出现明显空白块或错位。

## 数据影响

无 localStorage、路由或业务状态变更。

## 测试结果

已执行：

```bash
npm.cmd run build
npm.cmd run lint
```

结果：

- build：通过
- lint：通过
- 输出目录：`dist/`
- 输出文件：
  - `dist/index.html`
  - `dist/assets/index-BzBTzxaR.css`
  - `dist/assets/index-BuxC1IwM.js`

## 当前状态

Step 1 已完成。首页输入区目标开发文案已清理，核心输入和生成能力未受影响。

## 下一步建议

继续执行 Step 2：修复推荐任务完成后的空状态与刷新入口。
