# 1.0 已知问题（基线）

> 说明：此处为基线快照记录，不在本 step（2.0-00）内做业务修复。

## P0（阻塞）

- 无

## P1（影响主要交互/验收）

- 受限环境下运行 `vite` 可能出现 `spawn EPERM`（详见 `baseline_build_check.md`）

## P2（可后续优化）

- 部分中文文档在某些环境可能出现乱码（编码/字体/编辑器差异导致），需要统一 UTF-8 与编辑器设置
- 本地检索（RAG-ready）局限性：仅关键词/标签匹配，无法语义召回与重排（课堂 MVP 可接受，正式版需替换检索模块）
- 导出 JSON 可能受浏览器下载策略影响（下载被拦截时需要 UI 提示用户允许下载）
- 待购清单合并策略（同名/同类合并）仍有优化空间（当前以 `id` 去重为主）

