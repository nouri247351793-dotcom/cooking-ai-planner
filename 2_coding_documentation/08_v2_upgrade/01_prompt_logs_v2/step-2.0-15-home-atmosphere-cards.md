# Step 2.0-15 Prompt Log（Home Atmosphere + Aside Cards + Budget Modal）
## 输入 prompt 原文（用户）

当前任务是在现有 2.0 网站基础上进行一轮增量修正。

注意：
1. 不要重建项目，不要推翻现有技术路线。
2. 只处理本轮明确提出的问题，不要顺手大改其他页面。
3. 继续遵循《执行手册2.0（包含自归档）》：
   - 增量修改
   - 每轮只做一个明确步骤
   - 同步更新 prompt log、step report、issue log、flowchart、git 记录
   - 本轮结束后必须做最小验证（npm run build + dev server smoke check）
4. 当前阶段不接真实 AI，不新增云端能力，不改 mock provider 主路线。

====================
【本轮目标】
====================
完成“首页氛围与辅助卡片增强”，重点包括：
1. 适度增加 emoji / 轻图形符号
2. 重新设计首页右侧“学做饭小贴士”和“省钱计划”两张辅助卡片
3. “省钱计划”支持点击并弹出预算详情弹窗（轻量 mock 展示）

（其余“必须修改的具体要求 / 实现要求 / 归档要求 / 验证要求 / 输出顺序”同用户输入原文，此处不重复粘贴。）

